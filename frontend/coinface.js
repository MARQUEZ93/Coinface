import React from 'react';
import ReactDOM from 'react-dom';

document.addEventListener('DOMContentLoaded', () => {
  console.log("hit");
  const root = document.getElementById('root');
  ReactDOM.render(<h1>Welcome to Coinface!</h1>, root);
});
